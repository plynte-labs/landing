---
import { JSON_LD_GRAPH } from '@/data/seo';

interface Props {
  data?: Record<string, unknown>;
}

const { data = JSON_LD_GRAPH } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(data)} />
